% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_spline_basis.R
\name{build_spline_basis}
\alias{build_spline_basis}
\title{Build a B-spline basis for each subnational area}
\usage{
build_spline_basis(T_star, all_years = seq(1990, 2030.5, by = 0.5), nseg = 10)
}
\arguments{
\item{T_star}{Data frame returned by \code{compute_T_star()} with \code{average_year} and \code{index_subnat}.}

\item{all_years}{Numeric vector of years for which the basis is evaluated.}

\item{nseg}{Integer number of segments for interior knots (default 10).}
}
\value{
A list with elements:
\itemize{
\item B_ik A 3D array (n_subnat x length(all_years) x K) of spline basis values.
\item Kstar Integer vector of length n_subnat (effective K for each area).
\item knots_all Matrix of knot locations (n_subnat x K).
\item K Number of basis columns.
\item H K - 1 (used for difference penalties in smoothing).
}
}
\description{
For each subnational area this builds a b-spline basis matrix across \code{all_years}
using \code{bs_bbase_precise()} which is a small wrapper around \code{splines::bs()}.
The returned arrays mimic the shape used in your original script.
}
\examples{
# Example: Build B-spline basis 
library(dplyr)

# Create a small T_star dataset (as returned by compute_T_star)
T_star <- data.frame(
  index_country = c(1, 1, 2),
  index_subnat  = c(1, 2, 3),
  average_year  = c(2005, 2010, 2008),
  index_year    = c(2, 3, 2)
)

# Define years to evaluate the spline basis
all_years <- seq(2000, 2015, by = 1)

# Build spline basis
splines_out <- build_spline_basis(
  T_star    = T_star,
  all_years = all_years,
  nseg      = 5
)

}
