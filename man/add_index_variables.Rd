% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_index_variables.R
\name{add_index_variables}
\alias{add_index_variables}
\title{Create index variables for subnational modelling}
\usage{
add_index_variables(
  df,
  methods = c("Female Sterilization", "Implants", "Injectables", "IUD", "OC Pills"),
  all_years = seq(1990, 2030.5, by = 0.5)
)
}
\arguments{
\item{df}{A data frame that contains at least \code{Country}, \code{Region},
\code{Method} and \code{average_year} columns.}

\item{methods}{Character vector of methods in the desired order (default examples given).}

\item{all_years}{Numeric vector of years to index against (defaults to 1990–2030 by 0.5).}
}
\value{
The input data frame augmented with index columns.
}
\description{
Adds integer index columns for subnational area (\code{index_subnat}),
country (\code{index_country}), method (\code{index_method}) and year
(\code{index_year}) to facilitate modeling and JAGS data construction.
}
\examples{
# Clean the subnational data
subnat_clean <- clean_fp_source_data(subnat_bivar_data)
# Add the region, country, method and time indices
indexed_subnat <- add_index_variables(subnat_clean)

}
